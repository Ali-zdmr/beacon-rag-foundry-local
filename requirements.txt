numpy
flask

# Optional: enables real on-device inference via Microsoft Foundry Local.
# The app works without these two (falls back to an offline hashing embedder
# and an extractive responder) but generative answers require them.
foundry-local-sdk
openai
